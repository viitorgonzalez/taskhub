spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/taskhubdb
    username: taskhubuser
    password: taskhubpass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
